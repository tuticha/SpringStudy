<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="home">

	<select id="goLogin" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		select *
		from testMember
		where memberId = #{id}
			and memberPw = #{pw}
	</select>
	
	<select id="goLogin2" parameterType="java.util.HashMap" resultType="String">
		select memberId
		from testMember
		where memberId = #{id}
			and memberPw = #{pw}
	</select>
	
</mapper>